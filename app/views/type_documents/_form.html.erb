<%= form_with(model: type_document) do |form| %>
  <% if type_document.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(type_document.errors.count, "error") %> prohibited this type_document from being saved:</h2>

      <ul>
        <% type_document.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :code, style: "display: block" %>
    <%= form.number_field :code %>
  </div>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
